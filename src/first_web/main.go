package main

func main() {
	// http.ListenAndServe("", nil)
	// handler := MyHandler{}
	// server := http.Server{
	// 	Addr:    "127.0.0.1:8080",
	// 	Handler: &handler,
	// }

	// HelloHandler := HelloHandler{}
	// WorldHandler := WorldHandler{}

	// server := http.Server{
	// 	Addr: "127.0.0.1:8080",
	// }

	// http.Handle("/hello", &HelloHandler)
	// http.Handle("/world", &WorldHandler)

	// http.HandleFunc("/hello", Hello)
	// http.HandleFunc("/hello", log(Hello))
	// hello := HelloHandler{}
	// http.Handle("/hello", protect(log(hello)))
	// http.HandleFunc("/world", World)
	// server.ListenAndServe()

	// mux := httprouter.New()
	// mux.GET("/hello/:name", hello)

	// server := http.Server{
	// 	Addr:    "127.0.0.1:8080",
	// 	Handler: mux,
	// }

	// server.ListenAndServe()
	// RunHttp2Server()

	// server := http.Server{
	// 	Addr: "127.0.0.1:8081",
	// }

	// // http.HandleFunc("/headers", headers)
	// http.HandleFunc("/body", body)
	// http.HandleFunc("/process", process)
	// http.HandleFunc("/write", writeExample)
	// http.HandleFunc("/writeheader", writeHeaderExample)
	// http.HandleFunc("/redirect", handleExample)
	// http.HandleFunc("/json", jsonExample)
	// http.HandleFunc("/set_cookie", setCookie)
	// http.HandleFunc("/get_cookie", getCookie)
	// http.HandleFunc("/set_message", setMessage)
	// http.HandleFunc("/show_message", showMessage)
	// http.HandleFunc("/page1", page1)
	// http.HandleFunc("/page2", page2)
	// http.HandleFunc("/page3", page3)
	// http.HandleFunc("/action_process", action_process)
	// http.HandleFunc("/action_loop", action_loop)
	// http.HandleFunc("/action_set_action", set_action)
	// http.HandleFunc("/action_include_action", include_action)
	// http.HandleFunc("/do_format_date", do_format_date)
	// http.HandleFunc("/action_context_aware", context_aware)
	// http.HandleFunc("/form", form)
	// http.HandleFunc("/action_xss", action_xss)
	// http.HandleFunc("/define_action", define_action)
	// fmt.Println("listen Server:http://127.0.0.1:8081")
	// run()
	// file()
	// file_csv()
	// file_gob()
	// database()
	// database2()
	// database_for_sqlx()
	// store_for_gorm()
	// decode_xml()
	// decoder_decode_xml()
	// encode_xml()
	// decoder_encode_xml()
	// decode_json()
	// decoder_decode_json()
	// encode_json()
	// decoder_encode_json(
	runServer()
	// server.ListenAndServe()
}
